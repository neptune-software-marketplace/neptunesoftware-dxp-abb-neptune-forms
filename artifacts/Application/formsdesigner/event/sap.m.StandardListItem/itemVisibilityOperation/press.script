const context = oEvent.oSource.getBindingContext();
const data = context.getObject();

currentObject.visibleCondition = data.key;

modelpanTopProperties.refresh();
popVisibilityOperation.close();
