if (typeof _dataid === "undefined" || _dataid === null || _dataid === "") { return; } 
let loData = modelpanTopProperties.getData()._data;
let loConfig = modelpanTopProperties.getData()._config[loData.id];
if (loConfig.title) { 
    return loConfig.title; 
}
else {
    return loData.title;
}
