const context = oEvent.oSource.getBindingContext();
const data = context.getObject();


let caller = diaCacheObjectSelect.getCustomData().find((item) => item.getKey() === "caller");
if (typeof caller !== "undefined") {
    const field = caller.getValue();
    field.setValue(data.objectName);

}

diaCacheObjectSelect.close();
